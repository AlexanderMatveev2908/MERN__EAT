import { FC } from "react";
import { useScrollTop } from "../../../core/hooks/UI/useScrollTop";

const ManageSingleOrder: FC = () => {
  useScrollTop();

  return <div>ManageSingleOrder</div>;
};
export default ManageSingleOrder;
